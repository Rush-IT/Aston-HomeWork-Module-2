<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- JDBC параметры -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/users</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">qwe123</property>

        <!-- SQL диалект -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Выводить SQL в консоль -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.use_sql_comments">true</property>

        <!-- Стратегия обновления схемы -->
        <property name="hibernate.hbm2ddl.auto">validate</property>
        <!-- validate проверяет схему, update — обновляет; create — пересоздаёт -->

        <!-- Управление сессией -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Mapping entity -->
        <mapping class="com.userservice.entity.User"/>
    </session-factory>
</hibernate-configuration>
